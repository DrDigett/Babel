services:
  - type: web
    name: babel-plus-api
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: babel-plus-db
          property: connectionString
      - key: CORS_ORIGIN
        value: "*"
      - key: JWT_SECRET
        generateValue: true
      - key: GROQ_API_KEY
        value: PONER_KEY_AQUI

databases:
  - name: babel-plus-db
    databaseName: babel
    plan: free
