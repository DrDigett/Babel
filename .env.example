# Required: Groq API key
GROQ_API_KEY=

# JWT secret for auth tokens (change this in production)
JWT_SECRET=

# URL of the API (only needed if client runs separately, e.g. local dev)
# En producción con Render, el frontend y la API están en el mismo dominio
VITE_API_URL=http://localhost:3000
